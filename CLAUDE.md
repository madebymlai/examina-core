# qupled-core

**On session start:** Read `/home/laimk/git/qupled-cloud/.claude/rules.md` for all coding standards and repo selection guidance.

This repo contains the core business logic (analyzer, tutor, quiz engine, SM2, PDF processing, LLM prompts). The web layer lives in qupled-cloud.
